---
# Role tasks

- name: Deploy sqlserver jdbc connector
  include_role:
    name: amtega.artifact
  vars:
    artifact: "{{ sqlserver_jdbc_connector_artifact }}"
  tags:
    - role::sqlserver_jdbc_connector
